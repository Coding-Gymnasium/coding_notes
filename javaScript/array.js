// Methods
const myArray = [1,2,3,4];

// map();
myArray.map(el => el + 1)  // [2, 3, 4, 5]
myArray.map(() => "b")  // ["b", "b", "b", "b"]
myArray // [1, 2, 3, 4]

// filter();


// reduce();


// find();


// includes();,
